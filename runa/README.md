# runa

The runa is a 50% TUI based scenario executor. 
A scenario is a full game state that can be send directly to the battlesnake server by the `executor` to test the response.
The scenario can be created using the `creator`.